<html>
    <head>
        <style>
            canvas{
                background-color: rgb(195, 210, 210);
            }
            body{
                background-color: black;
            }
        </style>
    </head>
    <body>
        <canvas width="500" height="500" id="myCanvas"></canvas>
        <script>
            var canvas= document.getElementById('myCanvas');
            var ctx= canvas.getContext('2d');

            ctx.beginPath();
            ctx.font = "30px Arial";
            ctx.fillStyle = "blue";

            ctx.fillText("Hola mundo", 50, 100);

            ctx.closePath();

            ctx.beginPath();
            ctx.font = "40px Arial";
            ctx.fillStyle = "blue";

            ctx.strokeText("5 de al Pastor", 50, 200);

            ctx.closePath();

           



            

             ctx.beginPath();
             ctx.arc(240,380,50,0,2* Math.PI);
             ctx.fillStyle = "blue";
             ctx.fill();
